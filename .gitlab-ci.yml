image: ubuntu:16.04

stages:
    - build

before_script:
    - apt-get update && apt-get install -y git wget curl

build_job:
    stage: build
    script:
        - ./scripts/installUbuntuDeps.sh --cleanup --fast
        - ./scripts/installErizo.sh -eacdfs
        - ./scripts/installNuve.sh
        - ./scripts/installRecorder.sh
    artifacts:
        paths:
            - .
    only:
        - tags
